## Controlled vocabularies (frozen before pilot)

These JSON files define the controlled IDs used across the dataset.

Each file includes:
- `vocabulary_id`
- `version`
- `frozen_date`
- `items[]` (with `id`, Arabic label, and notes)

Do not change IDs after pilot starts; add new versions instead.


